const ctrlWrapper = require("./ctrlWrapper");
const validation = require("./validation");
const authMiddleware = require("./authMiddleware");

module.exports = {
    ctrlWrapper, validation, authMiddleware
}